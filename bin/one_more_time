#!/bin/bash

# Create a new project for a Jekyll gem called blah and work on it in VS Code

PROJECT_NAME=blah
PROJECT_ROOT="~/nugem_generated/$PROJECT_NAME"
rm -rf "$PROJECT_ROOT"
yes n | VO_DEBUGGING=x exe/nugem jekyll "$PROJECT_NAME"
"$PROJECT_ROOT/bin/setup"
code "$PROJECT_ROOT"
