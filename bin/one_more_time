#!/bin/bash

# Create a new project for a Jekyll gem called blah and work on it in VS Code

rm -rf generated/blah/
yes n | VO_DEBUGGING=x exe/nugem jekyll blah
~/nugem_generated/blah/bin/setup
code ~/nugem_generated/blah/
